# Lake — build cache and dependency checkout (regenerated by `lake build`)
/.lake

# VS Code — workspace recommendations and local settings
/.vscode/

# Cursor — local agent rules (not shared via git)
/.cursor/
