/-
# N-puzzle (sliding puzzle)

Definitions and proofs for the solvability criterion stated in `README.md`.
Start with fixed board sizes, then generalize to arbitrary `N × M`.
-/

-- Library builds cleanly before Mathlib imports are added module-by-module.
